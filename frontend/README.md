# Orchid Export Surplus Store — Frontend

## Environment
```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

## Development
```bash
npm run dev
```

## Backend Required
The frontend fetches product, cart, and order data from the Express backend. Ensure the backend is running on port 5000 before starting the frontend.
